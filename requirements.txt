numpy>=1.26,<2
pandas>=2.0
scipy>=1.10
# 行情抓取（update_data 三源熔断链：腾讯/东财走 requests，mootdx 走 TCP 7709）
requests>=2.31
mootdx>=0.11
# h5 数据集读写（daily_pv.h5 / result.h5）
pyarrow>=14.0
tables>=3.9
# factor_daily_compute 写 dfactor:{symbol}（可选，无 redis 时降级跳过缓存写入）
redis>=5.0
# ML 滚动训练（ml_train_rolling/ml_predict/ml_metrics）；缺失时服务照常
# 启动，训练工具返回明确错误。macOS 需先 brew install libomp
lightgbm>=4.3
# ── 可选：因子评估与组合分析增强（缺库时自动回退内置实现，不阻塞）──
# PyPortfolioOpt>=1.5  # portfolio_optimize method="mean_variance"（注意 PyPI 包名）
# hmmlearn>=0.3        # regime_detect 走 GaussianHMM
# ruptures>=1.1        # change_point 走 PELT
# arch>=6.0            # vol_forecast 走 GARCH(1,1)
# ── 可选：qlib 回测/数据导出（factor_backtest / gen_data / update_data）──
# pyqlib>=0.9.6   # Docker 构建自动处理（Dockerfile WITH_QLIB）：amd64 装官方
#                 # wheel；arm64 无 wheel 需 --build-arg WITH_QLIB=1 源码编译
