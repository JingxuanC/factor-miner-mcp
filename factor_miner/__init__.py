# factor_miner — Athena 原生因子挖掘（DESIGN-FACTOR-MINER.md）
# P0: gen_data（qlib 数据导出）+ sandbox（受限执行原语）
# P1: factor_eval（debug 评估 battery）+ factor_backtest（qlib 回测驱动）
#     + conf_combined_factors.yaml（回测配置模板）
